const button = document.getElementById('demoButton');
const output = document.getElementById('demoOutput');

button.addEventListener('click', () => {
  output.textContent =
    'A missed call becomes a transcript, urgency score, callback task, escalation rule, and booked-meeting metric in one client-branded workflow.';
});
